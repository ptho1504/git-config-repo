server:
  port: 8222
eureka:
  instance:
    # hostname: localhost
    hostname: discovery-server
  client:
    serviceUrl:
      # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      # defaultZone: http://discovery-server:8222/eureka/
      defaultZone: http://discovery-server:8222/eureka/
